### Zadanie 1
- Jest to mało rozwinięty projekt, więc za czasu zmieniłbym strukturę folderów w katalogu symfony-app, aby w przyszłości nie było z tym problemu.
- Dodatkowa naprawa namespace'ow oraz innych rzeczy (configi encji etc).
- Refactor większej ilości kodu, aby ujednolicić kod poprawnymi wzorcami (SQL w Controller'ach etc).
- Przebudowa LikeService'u na CommandBus'a
- Można dodać DTO do odpowiedzi, ale w takiej skali nie jest to konieczne.
- PS. Bardzo ładne zdjęcia w przykładzie. Uwielbiam patrzeć na takie rzeczy
